<i class="fa fa-comments"></i> Mensagens
</h1>
</div>
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title"><i class="fa fa-comments fa-fw"></i> Mensagens Recebidas</h3>
            </div>
            <div class="panel-body">
                <div id="morris-area-chart">
                  <table class="table table-striped">
                    <thead>
                      <th>Data</th>
                      <th>Nome</th>
                      <th>Email</th>
                      <th>Telefone</th>
                      <th>Mensagem</th>
                    </thead>
                    <tbody>
                      <% @messages.each do |message| %>
                        <tr>
                          <td><%= time_tag(message.created_at) %></td>
                          <td><%= message.name %></td>
                          <td><%= message.email %></td>
                          <td><%= message.phone %></td>
                          <td><%= message.message %></td>
                        </tr>
                      <% end %>
                    </tbody >
                  </table>
                  <div class="flickr_pagination">
                    <%= will_paginate @messages, previous_label: "Anterior", next_label: "Próximo" %>
                  </div>
                </div>
            </div>
        </div>
    </div>
</div>
