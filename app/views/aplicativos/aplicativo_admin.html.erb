<i class="fa fa-mobile"></i> Aplicativo
</h1>
</div>
</div>

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title text-muted"><i class="fa fa-quote-left fa-fw"></i> Dados da Tela Aplicativo no Momento</h3>
            </div>
            <div class="panel-body">
                <div id="morris-area-chart">
                  <% if @app.present? %>
                    <p><b>Data:</b> <%= time_tag(@app.created_at) %></p>
                    <p><b>Imagem Publicada:</b> <%= @app.avatar_file_name %></p>
                    <p><b>Texto:</b> <%= @app.app %></p>
                    <%= link_to edit_aplicativo_path(@app), class: 'btn btn-primary pull-right' do %>
                      <%= icon('edit') %> Editar Postagem
                    <% end %>
                  <% else %>
                    Sem postagem registrada
                  <% end %>

                  <%= link_to new_aplicativo_path, class: 'btn btn-success pull-right', data: { confirm: "Modificando a postagem o conteúdo atual da página Aplicativo será alterado" } do %>
                    <%= icon('plus') %> Atualizar Aplicativo
                  <% end %>

                </div>
            </div>
        </div>


    </div>
</div>
