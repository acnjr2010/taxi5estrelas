<i class="fa fa-home"></i> Home
</h1>
</div>
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title text-muted"><i class="fa fa-quote-left fa-fw"></i> Dados da Tela Home no Momento</h3>
            </div>
            <div class="panel-body">
                <div id="morris-area-chart">
                  <% if @home.present? %>
                    <p><b>Data:</b> <%= time_tag(@home.created_at) %></p>
                    <p><b>Imagem Publicada:</b> <%= @home.avatar_file_name %></p>
                    <p><b>Titulo:</b> <%= @home.title %></p>
                    <p><b>Texto:</b> <%= @home.texto %></p>
                    <%= link_to edit_home_path(@home), class: 'btn btn-primary pull-right' do %>
                      <%= icon('edit') %> Editar Postagem
                    <% end %>
                  <% else %>
                    Sem postagem registrada
                  <% end %>

                  <%= link_to new_home_path, class: 'btn btn-success pull-right', data: { confirm: "Modificando a postagem o conteúdo atual da página home será alterado" } do %>
                    <%= icon('plus') %> Atualizar Home
                  <% end %>

                </div>
            </div>
        </div>


    </div>
</div>
